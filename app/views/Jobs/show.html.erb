<%@job = Job.where(user_id: current_user.id)%>


 
<%= link_to 'Job Lists', jobs_path %> | <%= link_to 'Delete Job', @ship, method: :delete, data: { confirm: 'Are you sure want to delete this job?' } %>

